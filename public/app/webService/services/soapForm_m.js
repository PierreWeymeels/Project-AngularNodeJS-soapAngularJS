angular.module('soapForm_m', ['appMessage_m']).factory('soapForm', ['$log', 'appMessage',
function($log, appMessage) {
	
	
	
	//-----------------------------------------------------------------------

	return {
		
	};

}]);
