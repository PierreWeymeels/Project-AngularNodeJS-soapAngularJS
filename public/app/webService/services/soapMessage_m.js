angular.module('soapMessage_m', ['appMessage_m']).factory('soapMessage', ['$log', 'appMessage',
function($log, appMessage) {
	
	
	
	//-----------------------------------------------------------------------

	return {
		
	};

}]);
